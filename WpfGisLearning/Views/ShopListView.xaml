<UserControl x:Class="WpfGisLearning.Views.ShopListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:WpfGisLearning.Models"
             PreviewKeyDown="ShopListView_PreviewKeyDown">
    <UserControl.Resources>
        <Style x:Key="FilterComboBox" TargetType="ComboBox">
            <Setter Property="Height" Value="38" />
            <Setter Property="Padding" Value="12,0" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="#FFF7F5F2" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        </Style>
        <Style x:Key="FilterCheckBox" TargetType="CheckBox">
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Foreground" Value="#FF454B50" />
            <Setter Property="Margin" Value="0,0,8,0" />
        </Style>
        <Style x:Key="ShopCard" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Margin" Value="0,0,0,8" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5" />
            <Setter Property="RenderTransform"><Setter.Value><ScaleTransform ScaleX="1" ScaleY="1" /></Setter.Value></Setter>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                    <Setter Property="Background" Value="#FFFFF3ED" />
                    <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
                    <Setter Property="BorderThickness" Value="2" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ShopListItem" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="ListBoxItem"><ContentPresenter /></ControlTemplate></Setter.Value></Setter>
        </Style>
        <Style x:Key="FavoriteButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
            <Setter Property="Content" Value="☆" /><Setter Property="FontSize" Value="24" /><Setter Property="Padding" Value="4" /><Setter Property="Width" Value="36" /><Setter Property="Height" Value="36" />
            <Style.Triggers><DataTrigger Binding="{Binding IsFavorite}" Value="True"><Setter Property="Content" Value="★" /></DataTrigger></Style.Triggers>
        </Style>
        <Style x:Key="RegisterButton" TargetType="Button" BasedOn="{StaticResource AccentButton}">
            <Setter Property="Width" Value="104" /><Setter Property="Height" Value="40" /><Setter Property="BorderThickness" Value="0" /><Setter Property="Padding" Value="10,5" /><Setter Property="FontSize" Value="13" /><Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="DataManagementButton" TargetType="Button" BasedOn="{StaticResource IconButton}">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="76" />
            <Setter Property="Padding" Value="10,4" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Margin" Value="4,0,0,0" />
        </Style>
    </UserControl.Resources>
    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
        <Grid Grid.Row="0" Margin="0,14,0,10">
            <TextBlock Text="🗺 店舗を探す" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextBrush}" />
        </Grid>
        <Border Grid.Row="1" Background="{StaticResource PanelBrush}" CornerRadius="12" Padding="8,6" Margin="0,0,0,10" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <TextBlock Text="店舗データ" VerticalAlignment="Center" FontSize="12" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" />
                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="⇩ テンプレート" Click="DownloadExcelTemplateButton_Click" Style="{StaticResource DataManagementButton}" ToolTip="Excel入力テンプレートをダウンロード" />
                    <Button Content="↥ 書き出し" Click="ExportExcelButton_Click" Style="{StaticResource DataManagementButton}" ToolTip="店舗データをExcelに書き出す" />
                    <Button Content="↧ 読み込み" Click="ImportExcelButton_Click" Style="{StaticResource DataManagementButton}" ToolTip="Excelから店舗データを読み込む" />
                </StackPanel>
            </Grid>
        </Border>
        <Border Grid.Row="2" Background="{StaticResource SurfaceBrush}" CornerRadius="14" Padding="10" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                <Grid>
                    <TextBox x:Name="SearchTextBox" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}" Height="38" Padding="12,6" BorderThickness="0" Background="{StaticResource InputBackgroundBrush}" VerticalContentAlignment="Center" />
                    <TextBlock Text="店舗名・住所・ジャンルで検索" Foreground="#FFA0A6AB" VerticalAlignment="Center" Margin="12,0,8,0" IsHitTestVisible="False">
                        <TextBlock.Style><Style TargetType="TextBlock"><Setter Property="Visibility" Value="Collapsed" /><Style.Triggers><DataTrigger Binding="{Binding SearchKeyword}" Value=""><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers></Style></TextBlock.Style>
                    </TextBlock>
                </Grid>
                <Button Grid.Column="1" Content="リセット" Command="{Binding ClearSearchCommand}" Margin="8,0,0,0" />
            </Grid>
        </Border>
        <Border Grid.Row="3" Background="{StaticResource PanelBrush}" CornerRadius="14" Padding="10" Margin="0,0,0,12" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
            <StackPanel>
                <Grid Margin="2,0,2,8"><Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions><StackPanel Orientation="Horizontal"><TextBlock Text="FILTER" FontSize="11" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" /><TextBlock Text="条件で絞り込む" FontSize="12" Foreground="{StaticResource MutedBrush}" Margin="8,0,0,0" /></StackPanel><TextBlock Grid.Column="1" Text="SORT" FontSize="11" Foreground="{StaticResource MutedBrush}" /></Grid>
                <WrapPanel>
                    <ComboBox ItemsSource="{Binding RamenTypeOptions}" SelectedItem="{Binding SelectedRamenType}" Style="{StaticResource FilterComboBox}" Width="116" Margin="0,0,8,6" />
                    <ComboBox ItemsSource="{Binding PriceFilterOptions}" SelectedItem="{Binding SelectedPriceFilter}" Style="{StaticResource FilterComboBox}" Width="126" Margin="0,0,8,6" />
                    <CheckBox Content="お気に入り" IsChecked="{Binding FavoriteOnly}" Style="{StaticResource FilterCheckBox}" Padding="4,0" />
                    <CheckBox Content="現在地周辺" IsChecked="{Binding NearbyOnly}" Style="{StaticResource FilterCheckBox}" Padding="4,0" />
                    <ComboBox ItemsSource="{Binding NearbyRadiusOptions}" SelectedItem="{Binding SelectedNearbyRadius}" Style="{StaticResource FilterComboBox}" Width="92" Margin="0,0,14,6" />
                    <CheckBox Content="名前 A→Z" IsChecked="{Binding SortByName}" Style="{StaticResource FilterCheckBox}" Padding="4,0" />
                    <CheckBox Content="価格" IsChecked="{Binding SortByPrice}" Style="{StaticResource FilterCheckBox}" Padding="4,0" />
                    <CheckBox Content="評価" IsChecked="{Binding SortByRating}" Style="{StaticResource FilterCheckBox}" Padding="4,0" />
                </WrapPanel>
            </StackPanel>
        </Border>
        <Grid Grid.Row="4">
            <ListBox x:Name="ShopListBox" ItemsSource="{Binding ShopsView}" SelectedItem="{Binding SelectedShop, Mode=TwoWay}" Background="Transparent" BorderThickness="0" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Disabled" ItemContainerStyle="{StaticResource ShopListItem}" PreviewMouseDoubleClick="ShopListItem_PreviewMouseDoubleClick" Margin="0">
                <ListBox.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:Shop}">
                        <Border Style="{StaticResource ShopCard}">
                            <Border.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="詳細" Click="ShopContextMenu_Detail_Click" />
                                    <MenuItem Header="編集" Click="ShopContextMenu_Edit_Click" />
                                    <Separator />
                                    <MenuItem Header="お気に入り切替" Click="ShopContextMenu_Favorite_Click" />
                                    <MenuItem Header="削除" Click="ShopContextMenu_Delete_Click" />
                                </ContextMenu>
                            </Border.ContextMenu>
                            <Grid>
                                <Grid.ColumnDefinitions><ColumnDefinition Width="82" /><ColumnDefinition Width="*" /><ColumnDefinition Width="40" /></Grid.ColumnDefinitions>
                                <Border Grid.Column="0" Width="72" Height="82" Background="{StaticResource AccentSoftBrush}" CornerRadius="9" ClipToBounds="True" VerticalAlignment="Top"><Grid><TextBlock Text="🍜" FontSize="28" HorizontalAlignment="Center" VerticalAlignment="Center" /><Image Source="{Binding MainPhotoPath}" Stretch="UniformToFill" /></Grid></Border>
                                <StackPanel Grid.Column="1" Margin="10,0,0,0"><TextBlock Text="{Binding Name}" FontSize="17" FontWeight="Bold" Foreground="{StaticResource TextBrush}" TextTrimming="CharacterEllipsis" /><StackPanel Orientation="Horizontal" Margin="0,5,0,0"><Border Background="#FFE8EDF1" CornerRadius="6" Padding="7,3"><TextBlock Text="{Binding RamenType}" FontSize="11" Foreground="#FF53616C" FontWeight="SemiBold" /></Border><TextBlock Text="{Binding Rating, StringFormat=★ {0:F1}}" FontSize="12" Foreground="{StaticResource RatingBrush}" Margin="8,3,0,0" /></StackPanel><TextBlock Text="{Binding Address, TargetNullValue=住所未登録}" FontSize="12" Foreground="{StaticResource LabelBrush}" Margin="0,6,0,0" TextTrimming="CharacterEllipsis" /><TextBlock Text="{Binding Price, StringFormat=¥{0:N0}}" FontSize="15" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,7,0,0" /></StackPanel>
                                <Button Grid.Column="2" Style="{StaticResource FavoriteButton}" Command="{Binding DataContext.ToggleFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" HorizontalAlignment="Center" VerticalAlignment="Top" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <Button Content="＋  登録" Command="{Binding NewShopCommand}" Style="{StaticResource RegisterButton}" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,40,18" Panel.ZIndex="10" ToolTip="新しい店舗を登録" />
        </Grid>
    </Grid>
</UserControl>
