<Window x:Class="WpfGisLearning.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Ramenia"
        Width="1500"
        Height="900"
        MinWidth="1100"
        MinHeight="700"
        Background="#FFF9F4EF">

    <Window.Resources>
        <SolidColorBrush x:Key="AccentBrush" Color="#FFB83D2E" />
        <SolidColorBrush x:Key="AccentOrangeBrush" Color="#FFF28C28" />
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF9F4EF" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#FFE4D6CE" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="10,7" />
            <Setter Property="Margin" Value="3" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="HeaderButton" TargetType="Button">
            <Setter Property="Background" Value="#FFFFF7F1" />
            <Setter Property="Foreground" Value="#FF6C342A" />
            <Setter Property="BorderBrush" Value="#FFF0D6C8" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Padding" Value="14,8" />
            <Setter Property="Margin" Value="4,0,0,0" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="82" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#FF9F3D2E">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="🍜 Ramenia" Foreground="White" FontSize="30" FontWeight="Bold" />
                    <TextBlock Text="ラーメン店を地図から探す" Foreground="#FFFFE8DC" FontSize="12" Margin="2,1,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource HeaderButton}" Content="＋ 店舗を登録" Click="NewShopButton_Click" />
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Row="1" Margin="14">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="480" />
                <ColumnDefinition Width="14" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="White" CornerRadius="16" BorderBrush="#FFE9DED8" BorderThickness="1" ClipToBounds="True">
                <ContentControl x:Name="MainContent" />
            </Border>

            <Border Grid.Column="2" Background="White" CornerRadius="16" BorderBrush="#FFE9DED8" BorderThickness="1" ClipToBounds="True">
                <Grid>
                    <mapsui:MapControl x:Name="MapControl" />

                    <Border x:Name="InfoCardBorder"
                            Background="White"
                            CornerRadius="14"
                            Padding="16"
                            Width="300"
                            Visibility="Collapsed"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Bottom"
                            Margin="16"
                            BorderBrush="#FFE1CFC5"
                            BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock Grid.Row="0" Text="🍜 店舗情報" FontSize="12" Foreground="#FFB83D2E" FontWeight="Bold" />
                            <Button Grid.Row="0" Grid.Column="1" Content="×" Width="28" Height="28" Margin="0" Padding="0" Click="InfoCardClose_Click" />

                            <TextBlock Grid.Row="1" x:Name="InfoCardName" Text="店舗名" FontSize="21" FontWeight="Bold" Foreground="#FF332925" Margin="0,10,0,0" TextWrapping="Wrap" />
                            <StackPanel Grid.Row="2" Margin="0,10,0,0">
                                <TextBlock x:Name="InfoCardAddress" Text="住所" FontSize="13" Foreground="#FF6F5B53" TextWrapping="Wrap" />
                                <TextBlock x:Name="InfoCardPrice" Text="-" FontSize="18" FontWeight="Bold" Foreground="#FFB83D2E" Margin="0,8,0,0" />
                                <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                                    <Button Content="詳細を見る" Click="InfoCardDetail_Click" Padding="12,7" />
                                    <Button Content="編集" Click="InfoCardEdit_Click" Padding="12,7" Background="#FFF28C28" Foreground="White" BorderThickness="0" />
                                </StackPanel>
                            </StackPanel>
                        </Grid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
