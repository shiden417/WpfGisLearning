<Window x:Class="WpfGisLearning.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfGisLearning"
        xmlns:mapsui="clr-namespace:Mapsui.UI.Wpf;assembly=Mapsui.UI.Wpf"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="MainWindow"
        Height="450"
        Width="800">

    <Window.Resources>
        <SolidColorBrush x:Key="WindowBackgroundBrush" Color="#FFF7F5F2" />
        <SolidColorBrush x:Key="AccentBrush" Color="#FFB4552B" /> <!-- 暖色系アクセント -->
        <SolidColorBrush x:Key="CardBackground" Color="White" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Padding" Value="8" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>

        <Style x:Key="IconButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Background" Value="{StaticResource CardBackground}" />
            <Setter Property="BorderBrush" Value="#FFE6E6E6" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="16" />
            <Setter Property="Margin" Value="12" />
            <Setter Property="BorderBrush" Value="#FFECECEC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="SnapsToDevicePixels" Value="True" />
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource WindowBackgroundBrush}">
        <!-- Mapsui namespace -->
        <!-- Note: Mapsui.UI.Wpf assembly provides MapControl -->
        <!-- xmlns:mapsui をファイル先頭で宣言するため既存の xmlns 行を編集 -->
        <Grid.RowDefinitions>
            <RowDefinition Height="64" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="Transparent">
            <Grid Margin="16,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock Text="🍜 Ramenia" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" />

                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource IconButton}" ToolTip="検索">🔍</Button>
                    <Button Style="{StaticResource IconButton}" ToolTip="設定">⚙</Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Body -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <StackPanel Grid.Column="0" Margin="12" VerticalAlignment="Stretch">
                <Border Style="{StaticResource Card}" Padding="12">
                    <StackPanel>
                        <TextBlock Text="🏠 店舗" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8" />
                        <TextBlock Text="★ お気に入り" FontSize="14" Margin="0,0,0,8" />
                        <TextBlock Text="＋ 店舗追加" FontSize="14" Margin="0,0,0,8" />
                        <TextBlock Text="⚙ 設定" FontSize="14" />
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Main content area -->
            <Grid Grid.Column="1" Margin="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <!-- Left: Shop list (ContentControl に既存のビューを注入する) -->
                <Border Grid.Column="0" Style="{StaticResource Card}">
                    <ContentControl x:Name="MainContent" />
                </Border>

                <!-- Right: Map -->
                <Border Grid.Column="1" Style="{StaticResource Card}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <Grid>
                        <!-- Mapsui MapControl を配置 -->
                        <mapsui:MapControl x:Name="MapControl" />

                        <!-- 店舗情報カード（地図上に重ねて表示） -->
                        <Border x:Name="InfoCardBorder"
                                Background="White"
                                CornerRadius="8"
                                Padding="8"
                                Width="260"
                                Visibility="Collapsed"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Bottom"
                                Margin="12"
                                BorderBrush="#FFE0E0E0"
                                BorderThickness="1">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock x:Name="InfoCardName" FontWeight="SemiBold" FontSize="14" Text="店名" />
                                    <TextBlock x:Name="InfoCardAddress" FontSize="12" Text="住所" TextWrapping="Wrap" />
                                </StackPanel>

                                <Button Grid.Column="1" Content="×" Width="28" Height="28" VerticalAlignment="Top" HorizontalAlignment="Right" Click="InfoCardClose_Click" />
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>

</Window>