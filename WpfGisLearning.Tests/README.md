# WpfGisLearning.Tests

MSTest-based automated tests for behavior that can be verified without starting the WPF UI.

The first tests cover map coordinate validation, which is shared by the map UI and shop location handling.
